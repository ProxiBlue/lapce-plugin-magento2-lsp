[package]
name = "psp-types"
version = "0.1.1"
license = "Apache-2.0"
description = "Types for interaction with plugin server protocol (vendored fork pinned to lsp-types 0.94.x)"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
lsp-types = { version = "0.94", features = ["proposed"] }
